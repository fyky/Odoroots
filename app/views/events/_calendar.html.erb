  <%= month_calendar(attribute: :event_date, events: @allevents) do |date, allevents| %>
  <%= date %>

    <% @allevents.each do |event| %>
      <div>
        <%= event.name %>
      </div>
    <% end %>

<% end %>
