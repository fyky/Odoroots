<main>
  <h1>主催イベント一覧</h1>
    <table>
      <% @events.each do |event| %>
        <tr>
          <td><%= event.name %></td>

          <td><%= link_to "show", event_path(event.id) %></td>
        </tr>
      <% end %>
    </table>

</main>